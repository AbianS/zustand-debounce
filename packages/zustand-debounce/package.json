{
  "name": "zustand-debounce",
  "version": "2.0.0",
  "description": "",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "repository": "https://github.com/AbianS/zustand-debounce",
  "homepage": "https://github.com/AbianS",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup --config tsup.config.ts && tsc --emitDeclarationOnly --declaration",
    "dev": "tsc --watch"
  },
  "keywords": [
    "zustand",
    "debounce",
    "zustand-debounce",
    "zustand debounce"
  ],
  "author": "AbianS",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsup": "^8.3.0",
    "zustand": "^4.5.2"
  }
}
